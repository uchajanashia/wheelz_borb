# @tyre/api (placeholder)

Backend API for **tyre.ge**.

**Planned stack:** NestJS + PostgreSQL + Prisma.

This is an intentional placeholder — **not yet scaffolded**. The NestJS project will
be set up in a later task. It will expose the catalog/fitment data that the website
currently reads from static JSON fixtures, and will back the admin panel.

Nothing here is wired into the build yet.
